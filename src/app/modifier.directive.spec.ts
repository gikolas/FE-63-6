import { ModifierDirective } from './modifier.directive';

describe('ModifierDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifierDirective();
    expect(directive).toBeTruthy();
  });
});
